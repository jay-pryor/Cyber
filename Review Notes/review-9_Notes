1. With the Apply To in the Apply Control Mode, it currently applies the selected control to all items with a specific action setting, but can you make it so that if they all already have it, clicking it again will remove it from all of them. If only some of them have it however, it should add it to the rest of them.
2. In the control refs selection for each item in the tables, there is a big space between the left side of the box and the checkbox and another big space between the checkbox and the control name, so wide that you have to scroll rightwards to even see the name. Reduce the size of those spaces.
3. The generted implementation and maybe verification files have a generated timestamp which is in UTC, change that to AEST.
4. Could you also include a comment at the top of the implementation and verification files on how to run the ps1s. E.g. open command prompt via the address bar when in the adb folder, etc. and then the specific command prompt command required to run the current file. if that makes sense.
5. Can you also add the ability to name a project save file before it exports. Like maybe you click save project, and a popup appears where you enter the name of the file and then click confirm.