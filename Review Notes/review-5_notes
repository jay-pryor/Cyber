1. So under devices panel, I want the packages, settings and tactical lists to be collapsible. Also get rid of the control refs column in that view only. Then I want another section in the same tab below that has a list of controls that apply to that device config. For each control item in that list, I want to be able to click on it, and it will bring up its own packages, settings and tactical lists for the items that apply to both that device config and that control. The intent of this is so that there is a view that shows exactly what actions are taken to satisfy a specific control for a specific device config. ideally clicking on the control in this manner will bring up a pop up oage that will contain these lists that can then be closed via an x button in the top corner.
2. In the packages, settings and tactical tabs. When you add a control ref to an item, there is no way to remove it. You can also only add a single control reference. Some actions will help satisfy multiple controls, so this needs to be a multi select list not a single select.
3. For the importing of settings values into a device config, can you make it so the following format is also acceptible. A CSV with the columns: setting,description,value.
