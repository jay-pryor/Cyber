1. In the control selection menu for the apply control mode button, get rid of the control type beneath the name, just have the name only
2. Also in the apply control mode part, get rid of the explanation text "pick a control, then tick rows" and replace it with a new "Apply to" button that is only clickable if you have a control selected. When you press that button, it should open a drop down menu of all the possible actions (e.g. rmeove, keep, disable) and then select one, and it will apply that control to all items that have that action. The intent being to be able to apply a control to all items that have the remove action (e.g. for a debloating control). This only really applies to the packages table though, so don't implement this for settings or tactical.
3. In the devices tab, the packages and tactical lists have override columns which are changable - which is good, but the settings column doesn't. It should have one as well, maybe make the column widths indpeendently adjustable for each of those 3 tables too if possible. 