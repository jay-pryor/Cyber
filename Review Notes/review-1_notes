1. Add a darkmode toggle button please that changes everything to a darkmode.
2. I want to add a workflow for setting a device configuration to an existing set of files. So currently we onboard settings from files and then we device what to do with them later and that is good and we need that to stay like that. I want a separate workflow where after you have onboarded a device, you can set all the values to those already set in an existing set of files. It is important that all of the settings present in those files are the exact same (no less, no more) than the settings already applied to that device config, so maybe start with a validator that checks that they are all the same and if they are not refuses and gives a list of the deltas. If that passes with no issues, then update the decisions for that device config to match the files. This should be a workflow that can be done from the devices menu maybe. Actually make it a different button/workflow for packages, settings, and tactical. For settings and tactical its pretty straight forward, just copy the values from the .txt or json. For the packages though, I'm going to make the format for this a CSV where column 1 is the package name (itd be good if this would be resilient against having package: at the front or not), 2nd column is the action, and then the 3rd column is the description. Column names will be exactly:package,action,description. On the interface in the devices menu where this is done, explain the requirements for the input format for the packages input. For all of them, make it check that the format is correct first and refuse if it is not with appropriate error warnings.
3. I want another tab called Control Manager where you can add, remove, and edit controls. These can be of various types, but seed the type options with ISM, AHG, and Custom for now. Each control should have a title, a type, a description, and assigned device configs (because not all controls will apply to each config). The idea of this part is to give a way of viewing all the controls and what they are. Still make it so the assignment of decisions to controls is from the packages, settings, and tactical tabs though. But maybe rename the ISM Refs column & box to Control Refs, and make the box a search box that references the existing control list and you can select multiple controls to apply and their titles will be listed in the Control Refs column, instead of just an open ended text box.
4. Question: Explain to me what rationale and rollback do in each decision? I can intuitively understand the purpose of rationale, its just a place to put notes for other users of the app/yourself. What is the rollback section for though?
