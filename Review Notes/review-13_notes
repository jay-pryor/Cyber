1. With the Delete button, only the lighter shaded rows highlight in red when selected, make sure all rows highlight in red when selected.
2. Say if I link a control to 3 different items all at once with a single 'Apply Control Mode' command, if I press the undo button, it only undoes one of the entries at a time, I want it to undo all of the entries done in the last action at once. So if I apply a control to 3 items with the Apply Control Mode button at once, undoing it should remove that control from all 3 at once.
3. Add a redo button.
4. In the Control Manager tab, add a little interface at the top that lets you select which devices are displayed in the table below, and then for those that are to be displayed, they get their own column in the control manager table that is a checkbox, so that controls can be quickly applied and removed from specific devices rather than having to go into the drop down menu for each control and selecting the appropriate device there.
