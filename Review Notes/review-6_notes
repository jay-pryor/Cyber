1. For adding device configs as members to a group, could we make that a drop down select menu instead of checkboxes. the drop down menu should be toggle-able, so you drop downa dn select a device config to add it, and then you can do that again to remove it.
2. For the adding deviations to a group, can you make the pop-up window bigger (like covering almost the entire screen), it currently is hard t tell what deviations are assigned to the group as it is mashed together. Also the it should provide just the default value and then the deviation setting, not this type of thing: {"value":"0","type":"string"} →      (i.e. it should just be 0 in the Default column and then a setting column with the deviation value.
3. When adding a deviation, I like the select package, settings, or tactical, and then select the setting to deviate and then input the value, but can we make it so the setting is searchable when you do that, currently you have to scroll to find it and the list is quite long. 
4. Make the column width in the Control Manager tab table adjustable as well, similar to in the other tables.
5. In the packages, settings, and tactical tabs, when you access the dropdown to set Control refs, currently the name of the control ref is taking up two lines, despite there being more than enough space to write it on a single line, can we change that, maybe so the checkboxes are positioned at the left rather than in the middle.