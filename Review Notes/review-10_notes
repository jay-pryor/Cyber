1. Put in a warning on the generate tab next to the output scripts as txt not ps1 that HighCom network computers will not allow you to open a folder with ps1s and must be transferred to a non-HighCom machine before being relabelled as a .ps1 file.
2. The table in control manager is not the full screen width, it is limited. Can you extend it so it can cover the full screen if the columns widths are adjusted to be so.
3. Can we make it so we can also input settings files when onboarding in that same settings csv format we allow it for with the setting decisions for a device config. Basically I want to be able to import my descriptions from the onboarding process too.