# Source files allowed to exceed the 500-line cap in src/build.json.
#
# Format: <path relative to src/> <reason>
#
# The build FAILS on an oversized file that is not listed here, so joining this list is
# a deliberate, reviewable act rather than something that happens by drift. A file
# earns a place only when no clean seam exists inside it — a single function or
# statement longer than the cap, which a file boundary cannot cut. The fix is to break
# the function up; the exemption is what holds the line until someone does.
#
# The one entry below is an enormous wire() that binds every event handler for its view
# in a single function body. The Report Design view had the same shape and was split
# into per-concern wiring helpers (071-076 in its directory); do the same here when next
# working in App.ui.app, then delete its line from this file.

app/js/0460-ui-app/030-wire.js  751 lines: App.ui.app's wire() is one function — no seam inside it to cut on.
